name: Run Email Script

on:
  workflow_dispatch:

jobs:
  send_email:
    runs-on: ubuntu-latest
    env:
      EMAIL_USER: "mf0583275242@gmail.com"
      EMAIL_PASS: "gjzrznpbzvzfsmzw"
      EMAIL_TO: "m0544195828@gmail.com"
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run email script
        run: python vvv.py
